// app/uplink/page.tsx
import UplinkContainer from '@/components/uplink_page/UplinkContainer';

export const metadata = {
  title: 'Uplink | HOLYPING',
  description: '上行鏈路即時遙測（開發中）',
};

export default function UplinkPage() {
  return <UplinkContainer />;
}